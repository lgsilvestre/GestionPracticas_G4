import React from 'react';
import {ColorButton} from './styles';

const ButtonLanding = (props) => {

    return (
        <ColorButton variant="text"> {props.carrera}</ColorButton>           
    )
}

export default ButtonLanding
